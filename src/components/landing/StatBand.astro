---
// Stat band section
---
<!-- =====================================================================
     STAT BAND
     ===================================================================== -->
<section class="bg-gold on-colour">
  <!-- Only what a source document actually says. "150+ AI jobs targeted by
       Year 3" came from the Stakeholder Brief but is not a figure the Hub
       stands behind, so it is gone; "one contract party" appeared in no source
       at all. What is left is the five service lines the write-up lists, the
       launch date the summit itself publishes, and the surplus commitment the
       write-up states word for word. -->
  <div class="shell band-sm grid gap-x-8 gap-y-9 md:grid-cols-3 items-center">

    <div class="flex flex-col gap-2 reveal">
      <span class="stat-figure" data-count="5">0</span>
      <span class="text-[10px] font-extrabold tracking-[0.16em] uppercase text-[#4A3F18]">Service lines</span>
    </div>

    <div class="flex flex-col gap-2 md:border-l md:border-[#C9A343] md:pl-8 reveal" style="--d:90ms">
      <span class="stat-figure">Nov&nbsp;26</span>
      <span class="text-[10px] font-extrabold tracking-[0.16em] uppercase text-[#4A3F18]">Launch at GABS, Accra</span>
    </div>

    <p class="heading text-[clamp(17px,1.6vw,22px)] reveal md:border-l md:border-[#C9A343] md:pl-8" style="--d:180ms">
      Surpluses reinvested in<br>AI skills, jobs and businesses.
    </p>
  </div>
</section>
