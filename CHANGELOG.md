## Unreleased

- Fixed the GitHub Actions CI workflow to use the repository’s Node LTS version and aligned the non-blocking lint job with the project’s actual build command so the pipeline validates successfully.
